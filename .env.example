# Embedding provider configuration for local Ollama usage
EMBEDDING_PROVIDER=Ollama
OLLAMA_MODEL=llama3
EMBEDDING_MODEL=nomic-embed-text

# Milvus vector DB connection
MILVUS_ADDRESS=localhost:19530

# Optional: Override Ollama host if different from default
OLLAMA_HOST=http://localhost:11434
